#rdef main(){
    // Undefined behavior if copied
    world = "Hello World"
    // echo("Hello World!")

    // world = 1
    // echo(world)

    num = world
    echo(num)
    echo(num)
    echo(num)
}
#rdef_end
