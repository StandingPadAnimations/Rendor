#include "RendorInterpreter/Interpreter.hpp"

void Interpreter::CreateGVT()
{
    std::uint32_t VarCount = 0;
    (*File) >> VarCount;

    for (std::size_t var = 0; var < VarCount; ++var)
    {
        
    }
}